
# For each node that you need in your test scenario,
# your nodes file should have at least a "node" statement.
# For each node, you can request that additional repositories
# are enabled, and that specific packages are installed on top
# of the provisioned VM image.

node		client
install		coreutils
